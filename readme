* symbols:
main, ap_get_token,


* config:
sudo ln -s ../mods-available/languagesubdomain.load  /etc/apache2/mods-enabled/
apachectl


Need:
MultiViews ... notice that it's an option, and parent can AllowOverride None
1/ ModuleTutorialString "en"
must be option! This triggers this module!

